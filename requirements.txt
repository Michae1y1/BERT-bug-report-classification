# Project Dependencies - BERT Text Classification (PyTorch + Transformers)

pandas==2.2.3            # For loading and processing CSV data
numpy==2.1.3             # Numerical operations and array manipulation
torch==2.5.1             # PyTorch deep learning framework
scikit-learn==1.6.0      # Train/test splitting and classification metrics (accuracy, F1, AUC, etc.)
transformers==4.49.0     # HuggingFace library for BERT models and training pipeline
tokenizers==0.21.0       # Efficient tokenization backend used by Transformers
tqdm==4.67.1             # Progress bar support (used during training and evaluation)
nltk==3.9.1              # Natural Language Toolkit (used for text preprocessing or tokenization)
